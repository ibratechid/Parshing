# Copy this file to .env and fill in your real key for local development with `vercel dev`.
# In production (Vercel dashboard), set this as a Project Environment Variable instead —
# never commit the real key to git.
BAZAARLINK_API_KEY=sk-bl-your-key-here
